{
  "version": 1,
  "commands": [
    {
      "name": "ssh-agent",
      "aliases": [
        "ssh",
        "agent"
      ],
      "description": "Avvia ssh-agent nella shell corrente e aggiunge ~/.ssh/id_rsa",
      "mode": "eval",
      "tags": [
        "ssh",
        "setup"
      ],
      "notes": "Usa: eval \"$(comandi print ssh-agent)\" oppure abilita il wrapper e usa: c run ssh-agent",
      "steps": [
        {
          "run": "eval \"$(ssh-agent -s)\""
        },
        {
          "run": "ssh-add ~/.ssh/id_rsa"
        }
      ]
    },
    {
      "name": "copilot-secret-init",
      "aliases": [
        "secret",
        "copilot-secret"
      ],
      "description": "Inizializza un secret AWS Copilot con il profilo macnil-2",
      "mode": "exec",
      "tags": [
        "aws",
        "copilot"
      ],
      "env": {
        "AWS_PROFILE": "macnil-2"
      },
      "steps": [
        {
          "run": "copilot secret init"
        }
      ]
    }
  ]
}
